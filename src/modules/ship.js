// TODO: make this.length and this.hitsTaken private
export default class Ship {
  constructor(length) {
    this.length = length;
    this.hitsTaken = 0;
  }
  // this.length should have a setter asserting correct argument
  //   hit() {}

  //   isSunk() {}
}
